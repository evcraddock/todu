{
  "name": "@todu/sync-server",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "bun build ./src/server.ts --outdir ./dist --target node",
    "dev": "bun run --watch ./src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@todu/core": "workspace:*",
    "@automerge/automerge-repo": "^1.1.0",
    "@automerge/automerge-repo-network-websocket": "^1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
