{
  "name": "@todu/app",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "todu": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "dev": "bun run ./src/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@todu/core": "workspace:*",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
