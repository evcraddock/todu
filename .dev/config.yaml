# Development config — used by make run and make dev-electron
# Data is stored in .dev/data/ (gitignored) so dev never touches ~/.config/todu/
data_dir: data

sync:
  remote:
    server: ws://localhost:3030
