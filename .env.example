# Environment variables for local development
# Copy to .env and fill in values

# Data storage location (default: ~/.todu/data)
# TODU_DATA_DIR=~/.todu/data

# Automerge sync server (Phase 3+)
# TODU_SYNC_SERVER=ws://localhost:3030

# Debug mode
# DEBUG=todu:*
